# Placeholder: Pose detection logic using MediaPipe/BlazePose
import random

def detect_pose():
    # Simulate changing posture states
    return {
        "shoulder_angle": random.randint(70, 100),
        "head_tilt": random.randint(0, 30),
        "slouching": random.choice([True, False])
    }
